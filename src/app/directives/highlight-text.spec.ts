import { HighlightText } from './highlight-text';

describe('HighlightText', () => {
  it('should create an instance', () => {
    const directive = new HighlightText();
    expect(directive).toBeTruthy();
  });
});
